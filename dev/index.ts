import { createApp } from 'vue'
import App from './index.vue';
// import EData from '../src';
// import { EDigitalFlop, EFullScreenContainer } from '../src';

const app = createApp(App);

// app.use(EDigitalFlop);
// app.use(EFullScreenContainer);
// app.use(EData);

app.mount('#app');